# Skill 与 MCP 工具开发复盘

## 一、我为什么做这两个项目

### 1. Skill 的来源

我在公司开发过程中，领导经常强调一件事：

拿到一个复杂项目或者复杂需求后，不能马上让 AI 开始写代码。

需要先把整个项目和需求分析清楚，包括：

* 业务流程
* 数据流
* 数据库逻辑
* 模块之间的调用关系
* 可能受到影响的范围
* 代码应该按照什么顺序修改

如果没有分析清楚就直接写代码，AI 很容易自行发挥，最后写出来的代码可能能运行，但不符合真实业务逻辑，也达不到交付标准。

我先把领导讲的方法整理成了自己的 AI 开发方法论和开发步骤文档。后来我想到，可以把这些方法直接封装成一个 Skill，让 Codex 或 Claude Code 在处理复杂任务时自动按照这套流程进行分析。

因此，我开始开发这个 Skill。

我的目标不是单纯做一个公开项目，而是让它真正进入我的日常开发流程。以后遇到复杂任务时，我可以先使用 Skill 分析业务和代码，再让 AI 开始修改代码。

### 2. MCP 工具的来源

公司原来有一个工具，用来统计我们每天使用 Claude Code 和 Codex 的情况。

我看到这个工具后，想到能不能做一个属于自己的版本。

我希望它不仅能够统计使用量，还能够让我清楚知道：

* 今天使用了哪些 AI 编程工具
* 今天通过 AI 完成了什么任务
* 时间主要用在了什么地方
* 有没有进行无效尝试
* 有没有浪费时间
* 自己的 AI 使用方式是否合理

我还希望把这些数据接入 ChatGPT，让 ChatGPT能够根据我的使用记录，对我的工作和学习情况进行分析，帮助我提高自我认知。

所以，MCP 工具的本质不是单纯查看额度，而是记录和分析我的 AI 开发过程。

---

## 二、我的实际开发流程

这两个项目采用了不同的开发方式。

### 1. Skill：先整理方法，再进行实现

开发 Skill 之前，我已经有两份基础文档：

* AI 开发方法论
* AI 开发具体步骤

我先把自己的需求、方法和目标整理清楚，再告诉 AI：

* 我想把这套方法做成 Skill
* 这个 Skill 应该解决什么问题
* 我希望它最终按照什么流程工作

之后，我让 AI 分析具体应该怎么设计，先确定整体思路和开发计划，再开始推进实现。

完成初版后，我还使用不同的 AI 对 Skill 进行交叉评审。

不同 AI 会从不同角度指出问题，我再根据这些意见不断优化、修改和重新理解这套 Skill。

因此，Skill 的开发流程是：

> 先形成方法论，再设计方案，然后开发，最后通过多个 AI 交叉评审和持续迭代。

### 2. MCP：先完成初版，再围绕目标迭代

MCP 工具最开始是先快速完成一个初版。

完成初版以后，我再不断告诉 AI：

* 我还想增加什么能力
* 工具最终应该做成什么样
* 我真正想通过它解决什么问题
* 它应该如何接入 ChatGPT
* 用户应该如何启动和查看数据

AI 根据我的目标继续帮助我修改和完善，我负责确定方向、执行操作、检查结果。

因此，MCP 的开发流程是：

> 先做出可运行的初版，再根据目标持续修改，直到基本实现自己的使用需求。

---

## 三、开发过程中遇到的问题

整体来说，这两个项目并没有遇到特别难以解决的技术问题。

现在使用 AI 开发，很多原来看起来复杂的技术，真正开始做之后并没有想象中困难。

### 1. Skill 的主要问题：缺少真实使用验证

Skill 本身已经完成，但目前在真实复杂任务中的使用次数还不够多。

所以我还不能完全确认：

* 它的分析是否准确
* 它是否真的能够减少 AI 自行发挥
* 它是否能够降低返工
* 它的复杂度分级是否合理
* 中等任务和复杂任务的流程是否合适

它现在最大的问题不是写不出来，而是还需要在日常开发中长期验证。

### 2. MCP 的主要问题：ChatGPT MCP 接入

MCP 相对麻烦的部分是接入 ChatGPT。

接入过程中涉及：

* API 配置
* Tunnel ID
* Channel ID
* 本地启动
* 环境配置
* ChatGPT 端的连接配置

这些内容一开始比较陌生，操作步骤也比较多。

我的解决方式是让 AI一步一步告诉我应该怎么配置，然后我自己实际执行、查看结果、继续排错，最终完成接入。

---

## 四、这次做得比较好的地方

### 1. 会使用多个 AI 交叉验证

我没有完全相信某一个 AI 给出的第一次结果。

我会让不同 AI 分别评审我的 Skill，观察它们从不同角度提出的问题，然后再决定哪些建议值得采纳。

这种方式可以降低单个 AI 判断错误带来的风险。

### 2. 会持续优化，而不是生成一次就结束

在项目开发过程中，我会不断：

* 评审
* 修改
* 重新理解
* 再次测试
* 再让其他 AI 检查

我不是把 AI 生成的第一版直接当作最终结果，而是通过多轮迭代逐步完善。

### 3. 能够明确目标并推动 AI 完成交付

特别是在开发 MCP 时，我会告诉 AI：

* 我想做什么
* 为什么要做
* 最终结果应该是什么
* 当前结果和目标之间还差什么

AI 负责提供实现方案和代码，我负责确定方向、判断是否符合预期并持续推进。

### 4. 对陌生技术的信心提高了

以前我可能会觉得 Skill、MCP 这些东西比较复杂。

真正做完后，我发现这些概念没有想象中困难。

只要能够明确目标、拆分问题、借助 AI 学习和执行，我也能够把它们做出来。

这次经历提高了我使用 AI 进入陌生技术领域的信心。

---

## 五、这次做得不够好的地方

### 1. 对项目内部原理理解得还不够深入

虽然我能够借助 AI 把两个项目做出来，但我对一些内部实现、配置和原理还不够熟悉。

目前更接近：

> 能够使用、运行、调整和继续推进，但还不能完全脱离 AI，从零手写全部实现。

这说明项目虽然完成了，但其中部分能力还没有完全内化成我自己的能力。

### 2. 对代码和结构的掌控还不够强

如果让我完全依靠自己手写，我很难快速写出完整项目。

这并不意味着必须重新追求全部手写，但我至少需要逐步达到：

* 看得懂核心代码
* 说得清整体架构
* 知道每个模块的作用
* 能判断 AI 的设计是否合理
* 出现问题时能够定位大致范围
* 需求变化后知道应该修改哪里

### 3. Skill 还缺少实际效果证明

Skill 目前更多是在设计和评审层面完成了。

但它到底能够提升多少开发质量、减少多少返工，还需要真实复杂任务验证。

因此，现阶段不能仅凭文档完整和 AI 评审结果，就判断它已经完全成熟。

---

## 六、我如何判断项目是否成功

### 1. Skill 的成功标准

我希望 Skill 能够真正融入我的日常开发。

遇到简单需求时，不需要启动复杂流程，让 AI 直接完成。

遇到中等任务时，执行一套适中的分析流程。

遇到复杂需求时，再完整分析：

* 业务流程
* 数据流
* 数据库逻辑
* 模块关系
* 修改范围
* 风险
* 实施步骤

如果分析完成后，后续写代码比较顺畅，很少出现方向错误和大规模返工，那么这个 Skill 就是成功的。

### 2. MCP 的成功标准

只要我当天高强度使用 Claude Code 或 Codex，就可以通过 MCP 清楚知道：

* 我今天做了什么
* AI 帮我完成了什么
* 时间主要花在哪里
* 有没有无效操作
* 有没有明显浪费
* 哪些地方以后可以优化

如果它能够长期帮助我回顾自己的 AI 使用过程，促进自我认知和效率提升，那么它就是成功的。

### 3. 外部关注是额外价值

这两个项目首先是为了自己长期使用。

如果未来它们能够获得：

* GitHub Star
* 其他开发者使用
* 技术社区关注
* 求职中的认可
* 个人影响力

这些也算成功，但不是最核心的成功标准。

首要标准始终是：它们是否真正对我长期有用。

---

## 七、我希望形成的长期习惯

### 1. 使用 Skill 的习惯

以后遇到复杂开发任务时，不再马上让 AI 写代码。

先使用 Skill 对项目和需求进行分析，确认：

* 要解决什么问题
* 涉及哪些模块
* 数据如何流转
* 数据库如何变化
* 修改可能影响哪里
* 最终结果应该是什么

分析清楚之后，再开始编码。

我希望这逐渐变成我的默认开发习惯。

### 2. 使用 MCP 的习惯

只要当天高强度使用 Claude Code 或 Codex，就查看一次 MCP 数据。

查看数据后，需要分析：

* 今天最有价值的工作是什么
* 是否存在重复尝试
* 是否在无关问题上浪费时间
* AI 有没有帮我提高效率
* 哪些使用方式需要调整

MCP 的作用是把原本模糊的工作过程变成可以回顾和分析的数据。

---

## 八、我对 AI 时代程序员能力的新认识

我认为，单纯手写代码的速度已经不再是最重要的竞争力。

例如，一个 SQL 语句，我自己可能要写半个小时，而 AI 可能一分钟就能完成。

如果 AI 能够更快、更准确地完成标准编码任务，就没有必要为了证明自己而强行手写所有代码。

但是，基础知识仍然非常重要。

如果不懂后端知识、数据库、业务逻辑和系统设计，就无法：

* 判断 AI 生成的代码是否正确
* 发现权限和安全问题
* 判断数据是否一致
* 识别系统设计缺陷
* 处理复杂业务逻辑
* 控制 AI 的输出方向

我认为，未来真正能够拉开程序员差距的能力包括：

1. 积极拥抱新技术
2. 扎实的后端和计算机基础
3. 产品化思维
4. 系统设计能力
5. 清楚定义问题的能力
6. 明确最终目标和验收标准
7. 判断 AI 方案是否合理的能力
8. 调度 AI 完成交付的能力

程序员的价值不再主要体现在亲自写了多少代码，而在于：

> 能否理解问题、设计系统、确定目标、评审方案，并利用 AI 高质量地完成交付。

---

## 九、下一次开发类似工具时的原则

下一次再做 Skill、MCP 或其他 AI 工具，我不会为了做项目而做项目。

我会等到真实需求出现后，再决定是否开发。

开发之前，我会先把以下内容说清楚：

* 我遇到了什么具体问题
* 为什么需要这个工具
* 工具面向什么场景
* 我希望它最终做到什么程度
* 哪些功能是核心
* 最终验收结果是什么

然后让 AI 先提供整体思路和实施方案。

我负责评审这个思路：

* 是否符合真实需求
* 是否过度设计
* 是否遗漏关键问题
* 最终结果是否和我的设想一致

确认方案后，再推进开发。

至于具体需要改变哪些流程，不能提前机械确定。不同项目应该根据实际情况进行判断。

---

## 十、阶段总结

这次完成 Skill 和 MCP，对我最大的意义不是增加了两个 GitHub 项目，而是让我进一步确认了一种开发方式：

> 我负责发现问题、明确目标、设计结果和评审方案，AI 负责提高实现效率。

我也认识到：

* 新技术没有想象中神秘
* 借助 AI 可以快速进入陌生领域
* 能做出来不代表已经完全掌握
* 真正的能力仍然需要通过长期使用和验证形成
* 项目的长期价值取决于它是否真正融入我的工作习惯

接下来，我不需要马上继续开发新的 Skill 或 MCP。

更重要的是在真实开发中持续使用这两个工具，通过实际结果判断它们哪里有效、哪里需要优化，并逐步把项目背后的知识和方法真正内化成自己的能力。
